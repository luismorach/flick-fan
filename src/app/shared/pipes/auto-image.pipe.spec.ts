import { AutoImagePipe } from './auto-image.pipe';

describe('AutoImagePipe', () => {
  it('create an instance', () => {
    const pipe = new AutoImagePipe();
    expect(pipe).toBeTruthy();
  });
});
