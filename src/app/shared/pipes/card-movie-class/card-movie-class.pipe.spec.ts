import { CardMovieClassPipe } from './card-movie-class.pipe';

describe('CardMovieClassPipe', () => {
  it('create an instance', () => {
    const pipe = new CardMovieClassPipe();
    expect(pipe).toBeTruthy();
  });
});
