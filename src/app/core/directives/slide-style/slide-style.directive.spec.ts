import { SlideStyleDirective } from './slide-style.directive';

describe('SlideStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideStyleDirective();
    expect(directive).toBeTruthy();
  });
});
