import { HoverSerieExpandDirective } from './hover-serie-expand.directive';

describe('HoverSerieExpandDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverSerieExpandDirective();
    expect(directive).toBeTruthy();
  });
});
